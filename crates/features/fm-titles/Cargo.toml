[package]
name = "fm-titles"
version.workspace = true
edition.workspace = true
license.workspace = true
rust-version.workspace = true

[dependencies]
# Pinned exactly: glyph coverage is this dependency's output, and a different
# 0.2.x may tessellate or accumulate differently. See the determinism notes in
# `src/render.rs` - reproducibility is scoped to one pinned build.
ab_glyph = "=0.2.32"
fm-types = { path = "../../foundation/fm-types" }
fm-video = { path = "../../media/fm-video" }

[lints]
workspace = true
